# This Python code snippet defines three variables: `name` with the value "Miks", `goal` with the
# value "To create an AI that handles agri", and `wifi` with the boolean value False.
name = "Miks"
goal = "To create an AI that handles agri"
wifi = False
print("Name: ", name)
print("Goal: ", goal)
print("Does it require WI-FI: ", wifi)
